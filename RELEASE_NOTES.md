# CMCC Linux Docker v1.5.9 no-cooldown

- Restored SDK-first keepalive chain without the cloud-card readiness gate.
- Removed the five-minute account recovery failure cooldown.
- Failed recovery is immediately eligible for the next scheduled recovery round.
- Fixed 10-second probes, central six-slot scheduler, SDK then priority click fallback remain enabled.
